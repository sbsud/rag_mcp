_format_version: "3.0"
_transform: true

services:
  - name: agent-service
    url: http://host.docker.internal:8080
    routes:
      - name: agent-route
        paths:
          - /query
        methods:
          - POST